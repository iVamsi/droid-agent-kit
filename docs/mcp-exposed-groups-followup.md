# MCP exposed groups follow-up

Wire CLI through mcpDispatcher() or default DroidAgentMcpDispatcher exposedGroups from config.resolvedExposedToolGroups().

Also add junit-bom-5.12.2.module to gradle/verification-metadata.xml for installDist.
