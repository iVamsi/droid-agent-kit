diff --git a/cli/src/main/kotlin/com/droidagentkit/cli/DroidAgentMain.kt b/cli/src/main/kotlin/com/droidagentkit/cli/DroidAgentMain.kt
--- a/cli/src/main/kotlin/com/droidagentkit/cli/DroidAgentMain.kt
+++ b/cli/src/main/kotlin/com/droidagentkit/cli/DroidAgentMain.kt
@@ -9,7 +9,6 @@
 import com.droidagentkit.core.Json
 import com.droidagentkit.core.ResultStatus
 import com.droidagentkit.inspector.AndroidProjectInspector
-import com.droidagentkit.mcp.DroidAgentMcpDispatcher
 import com.droidagentkit.mcp.DroidAgentMcpHttpServer
 import com.droidagentkit.mcp.DroidAgentStdioServer
 import com.droidagentkit.mcp.DroidAgentWorkspaceDispatcher
@@ -141,7 +140,7 @@
     private fun serveMcp(command: CliCommand.ServeMcp): Int {
         val projectRoot = ProjectLocator.resolve(command.project)
         val config = resolveServerConfig(DroidAgentConfigLoader.load(projectRoot)) { System.err.println(it) }
-        val projectDispatcher = DroidAgentMcpDispatcher(config, projectRoot)
+        val projectDispatcher = mcpDispatcher(config, projectRoot)
         val dispatcher =
             command.projectsRoot?.let { configuredRoot ->
                 val projectsRoot = Path.of(configuredRoot).toAbsolutePath().normalize()
@@ -152,7 +151,7 @@
                 DroidAgentWorkspaceDispatcher(projectsRoot, projectDispatcher) { root ->
                     val projectConfig =
                         resolveServerConfig(DroidAgentConfigLoader.load(root)) { System.err.println(it) }
-                    DroidAgentMcpDispatcher(projectConfig, root)
+                    mcpDispatcher(projectConfig, root)
                 }
             } ?: projectDispatcher
         if (command.transport == "stdio") {
@@ -203,7 +202,7 @@
                     return 1
                 }
             }
-        val result = DroidAgentMcpDispatcher(config, root).call("android_devices_list", mapOf("rootPath" to root.toString()))
+        val result = mcpDispatcher(config, root).call("android_devices_list", mapOf("rootPath" to root.toString()))
         val adbOutput = readAdbDevicesOutput(result)
         val output =
             if (command.format == "markdown") {
@@ -241,7 +240,7 @@
                     return 1
                 }
             }
-        val result = DroidAgentMcpDispatcher(config, root).call(tool, args + ("rootPath" to root.toString()))
+        val result = mcpDispatcher(config, root).call(tool, args + ("rootPath" to root.toString()))
         println(Json.write(result))
         return if (result["status"] == "failed" || result["status"] == "blocked") 2 else 0
     }
diff --git a/mcp-server/src/main/kotlin/com/droidagentkit/mcp/DroidAgentMcpDispatcher.kt b/mcp-server/src/main/kotlin/com/droidagentkit/mcp/DroidAgentMcpDispatcher.kt
--- a/mcp-server/src/main/kotlin/com/droidagentkit/mcp/DroidAgentMcpDispatcher.kt
+++ b/mcp-server/src/main/kotlin/com/droidagentkit/mcp/DroidAgentMcpDispatcher.kt
@@ -73,7 +73,7 @@
     override val config: DroidAgentConfig,
     projectRoot: Path = Path.of("."),
     private val inspector: AndroidProjectInspector = AndroidProjectInspector(),
-    private val exposedGroups: Set<ToolGroup> = setOf(ToolGroup.CORE),
+    private val exposedGroups: Set<ToolGroup> = config.resolvedExposedToolGroups(),
     private val extraProviders: List<McpToolProvider> = emptyList(),
 ) : McpDispatcher,
     DeviceToolContext {
diff --git a/gradle/verification-metadata.xml b/gradle/verification-metadata.xml
--- a/gradle/verification-metadata.xml
+++ b/gradle/verification-metadata.xml
@@ -932,6 +932,9 @@
          <artifact name="junit-bom-5.12.2.pom">
             <sha256 value="cef80fec86454f6806bfb0df24669b5c6f32e2cb728539ea859f47dfdc9bbc17" origin="Generated by Gradle"/>
          </artifact>
+         <artifact name="junit-bom-5.12.2.module">
+            <sha256 value="de70ac5d91a52656d8890a6d23b9e04d99b99b1a0402530decad71bf31b2735e" origin="Generated by Gradle"/>
+         </artifact>
       </component>
       <component group="org.slf4j" name="slf4j-api" version="2.0.7">
